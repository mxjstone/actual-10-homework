# coding=utf-8import oslog_file = open('d:/51reboot/github/actual-10-homework/03/yangjing/access.log')log_content = log_file.readlines()ip_address = []for x in range(len(log_content)):	record = log_content[x].split(' ')	ip_address.append(record[0])print ip_addressn = 0log_dict = {}for x in ip_address:	if x in log_dict:		log_dict[x] += 1	else:		log_dict[x] = 1log_ipaddress = [(v, k) for k, v in log_dict.items()]log_dict = dict((k, v) for k, v in log_ipaddress)for x in log_dict.keys():	print '%s %s' % (x, log_dict[x])print len(log_dict)items = 0count_file = open('d:/51reboot/github/actual-10-homework/03/yangjing/count.html', 'a+')count_file.write("<html><table style='border:solid 1px'>")count_file.write("<th style='border:solid 1px'>counter</th><th style='border:solid 1px'>IP address</th>")# while items < 4:for k in log_dict.keys():	count_file.write(			'<tr><td style="border:solid 1px">%s</td><td style="border:solid 1px">%s</td></tr>' % (k, log_dict[k]))items += 1count_file.write("</table></html>")count_file.close()